# Instrumented webcam acquisition

Acquisition is isolated behind `FrameSource` and terminates at an owned immutable `RawFrame` payload. `AcquisitionService` adds monotonic receipt timing, bounded buffering, sequence/drop accounting, queue depth, stage spans, and inter-frame interval metrics. It does not know about hands, landmarks, gestures, scenes, or rendering.

`SyntheticFrameSource` is the deterministic test source. `MacOSAVFoundationSource` launches the native `native/CameraCapture.swift` AVFoundation helper and converts its length-delimited BGRA protocol into `RawFrame`; it never fabricates frames. The helper must be built on macOS. The hardware smoke entry point is `tools/acquisition_smoke.py`, intentionally excluded from normal tests.

The helper is packaged as `native/CameraCapture.app` with a stable bundle identifier and `NSCameraUsageDescription` in its bundle `Info.plist`, which are required for macOS TCC camera authorization. The authorization state is checked before session setup; denied/restricted/not-authorized states produce structured errors and do not emit frames. Once started, the helper is a long-lived `NSApplication` host: one `AVCaptureSession` and one bounded stdout stream remain active until Python requests shutdown. SIGTERM/SIGINT stop the delegate and session explicitly; the JSON-header/length-prefixed BGRA protocol is unchanged.

The acquisition buffer is bounded and can use latest-observation-wins. Every eviction is counted. Raw frame bytes are owned immutable `bytes` at the contract boundary; no native buffer lifetime is exposed downstream. Native capture timestamps are CMSampleBuffer media-time values (`avfoundation-media-time`, nanoseconds derived from CMTime), while application receipt/queue timestamps use the Python monotonic clock and are never subtracted from source timestamps. Source and receipt times therefore remain separate; cross-clock capture latency is unavailable until an explicit correlation is established. Native BGRA stride and payload size are validated and retained in metadata, so padded rows are not interpreted as tightly packed data. Requested camera rate is configuration metadata only; actual delivery is derived from source frame timestamps.

This milestone does not claim webcam availability, M5 measurements, delivered FPS, low latency, or real-time performance.

Current target validation: the native helper builds as arm64 Mach-O on the MacBook Air M5. The first real smoke execution delivered zero frames because macOS reported `camera authorization denied` (helper exit code 20) before session startup. This is a permission failure, not camera-delivery evidence. FPS, modes, payload measurements, and sustained behavior remain unavailable until camera access is granted through macOS privacy controls.

Latest target validation: camera authorization is now granted. The one-second smoke test reported `REAL_HARDWARE` with 18 frames. A separate five-second short acquisition run received 94 frames from `MacBook Air Camera` (source ID `6C707041-05AC-0011-0003-000000000001`), with 1920x1080 BGRA8, bytes-per-row 7680, payload size 8294400, source domain `avfoundation-media-time`, and origin `CMSampleBuffer presentation timestamp`. It observed 18.689 FPS by wall-duration count, 93 intervals with mean 33.727 ms, median 33.340 ms, p95 33.340 ms, p99 39.240 ms, minimum 16.667 ms, maximum 83.353 ms, and zero sequence gaps. These are short-run acquisition observations only, not sustained/system-wide performance claims. Capability probing reported seven formats: 640x480, 1280x720, 1760x1328, 1920x1080, 2560x1440, 3840x2160, and 4096x2304, each with a reported 15-30 FPS range and media subtype 875704438.

The earlier five-second artifact above used the pre-correction wall-rate-only schema and is superseded for FPS methodology. The corrected runner now reports wall-duration receipt rate separately from source timestamp-span cadence: `(last_source_timestamp - first_source_timestamp)` and `(frame_count - 1) / source_timestamp_span`. It also reports mean reciprocal interval, interval percentiles, and population standard deviation of source intervals. A corrected repeat, consumer-lag run, and 30-second run attempted on 2026-09-04 were blocked before frame delivery by `CAMERA_ERROR code=20 message=camera authorization denied` after rebuilding the helper, so no corrected repeat or stability result is claimed. The previously successful run remains historical evidence only and must not be treated as a result from the corrected schema.

Latest evidence attempt: the corrected five-second run, consumer-lag run, and 30-second run each failed before the first frame with `CAMERA_ERROR code=20 message=camera authorization denied`. Their artifacts are under `benchmarks/results/`. No fresh cadence, drop, lag, or stability result is claimed.

## Fresh signed-helper validation (2026-09-05)

The helper was rebuilt and signed with the Apple Development identity and the required hardened-runtime camera entitlement. Two consecutive one-second smoke tests delivered real hardware frames (1 and 19 frames). The corrected runs below are the current evidence; earlier results remain historical.

- Five-second run, `72ff3695-ea62-4465-b572-6481df0848e0`: 118 frames over 5.054160 s; wall receipt rate 23.347 FPS; source timestamp span 4.586710 s and cadence 25.508 FPS. Mean interval 39.203 ms, median/p95 40.170 ms, p99 47.010 ms, range 16.667–50.013 ms, population interval standard deviation 3.380 ms. Sequences 0–117, zero observed gaps, buffer maximum depth 1, zero evictions.
- Consumer-lag run, `5a3b3b63-7548-4be8-a686-a999f76c5aea`: 10 frames over 2.059899 s; wall receipt rate 4.855 FPS; source cadence 9.611 FPS. Mean interval 104.043 ms, median 133.340 ms, p95/p99 136.310 ms, population standard deviation 40.911 ms. Sequences 0–9, zero observed gaps, buffer maximum depth 1, zero recorded evictions. These results show consumer pacing; they do not prove camera-level drops.
- Thirty-second run, `caecd8f0-c34a-4413-a205-ba9af6f1b4fc`: 888 frames over 30.054903 s; wall receipt rate 29.546 FPS; source cadence 29.960 FPS. Mean interval 33.378 ms, median/p95 33.340 ms, p99 33.340 ms, range 16.667–83.343 ms, population standard deviation 1.783 ms. Sequences 0–887, zero observed gaps, buffer maximum depth 1, zero evictions. This is short acquisition stability evidence, not final sustained or thermal characterization.

All runs observed `MacBook Air Camera`, 1920x1080 BGRA8, 7680 bytes/row, 8294400-byte payload, source domain `avfoundation-media-time`, and CMSampleBuffer presentation timestamps. Requested configuration remains 640x480 BGRA with `high-default`; actual delivered dimensions remain 1920x1080. CPU user/system time and peak RSS were measured; GPU and thermal data remain unavailable. Artifacts retain full interval samples and metadata under `benchmarks/results/`.

## Startup-versus-steady-state characterization (2026-09-05)

Artifact `benchmarks/results/acquisition_startup_steady_10s.json` records a targeted 10-second repeat with a one-second startup analysis window. It received 239 frames over 10.077 s, with source cadence 25.583 FPS and wall receipt rate 23.718 FPS. The first one-second source interval subset had mean 34.634 ms, while the remaining 210 intervals had mean 39.682 ms and median 40.170 ms. Thus the lower cadence was not explained solely by the first-second startup window; most of the run remained near 25 FPS. This contrasts with the separate 30-second run near 29.96 FPS, so the cause is a real run-to-run acquisition condition difference not resolved by this targeted test. No camera-level drop is inferred: sequences were continuous and the bounded buffer recorded no evictions. Further controlled mode/environment comparison is required before freezing a single cadence baseline.

## Controlled configuration comparison (2026-09-05)

Recovery status: subsequent authorization-denied attempts occurred before capture-session creation and are not performance measurements. The historical seven-format capability record is preserved; current enumeration is unavailable while authorization is denied. After authorization is independently restored, run one capability query, then at least three unchanged default `.high` sessions while retaining native negotiated-format/frame-duration diagnostics and per-run cadence, jitter, sequence, buffer, and resource artifacts. Compare near-25 FPS and near-30 FPS sessions by negotiated state and runtime conditions; treat any relationship as correlation until reproduced. Acquisition remains unfrozen until this procedure is completed.

The runner now supports an opt-in requested frame rate; the default remains unchanged. With the same device, preset, pixel format, and environment, the default `.high` run (`benchmarks/results/acquisition_mode_default_10s.json`) delivered 274 frames, source cadence 29.661 FPS, wall receipt rate 27.290 FPS, median/p95/p99 interval 33.340 ms, jitter 3.721 ms, zero sequence gaps, and zero buffer evictions. The explicit 30 FPS frame-duration run (`benchmarks/results/acquisition_mode_explicit30_10s.json`) delivered 287 frames, source cadence 29.892 FPS, wall receipt rate 28.631 FPS, median/p95/p99 interval 33.340 ms, jitter 3.115 ms, zero sequence gaps, and zero buffer evictions. Both delivered 1920x1080 BGRA8. The explicit request was accepted by the native helper. This does not identify the cause of the earlier 25 FPS runs; it establishes only that both tested configurations can deliver near-30 FPS under this condition.
